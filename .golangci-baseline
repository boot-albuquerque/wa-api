# Gate de lint. Duas medidas, só uma é trava.
#
# max_complexity — a maior complexidade ciclomática de UMA função no repo.
#   Esta é a trava: `make lint` falha se ela SUBIR. Uma função gigante é uma
#   função gigante, independente do que existe em volta dela.
#
# count — quantas issues o lint reporta. INFORMATIVO, nunca trava.
#   Contagem de issues pune decomposição: quebrar um monolito de complexidade
#   202 em cinco funções de 40 reduz a complexidade real em todo lugar e
#   *aumenta* a contagem de 1 para 5. Travar na contagem é premiar quem deixa
#   o monolito de pé. (Achado da revisão de arquitetura da Fase 7.)
max_complexity=56
# vendor-wa-noise-native-fork (2026-08-06): 105->87. golangci-lint fmt
# (goimports) reagrupou os imports nos 45 arquivos consumidores após a
# troca mecânica de import path pra internal/wa-noise/ — não é código
# novo, é reformatação de import block. max_complexity inalterado (56).
# Quebra de ClientManager em sub-registries (2026-08-07): 283 -> 261.
# max_complexity inalterado (56) — a quebra nao tocou a funcao mais complexa
# do repo, que vive em internal/wa-noise/. A queda de 22 issues vem de
# reescrever os cinco arquivos de metodos de ClientManager como quatro
# pacotes com estado proprio: sumiram os acessos diretos a mapa compartilhado
# que o lint sinalizava.
# Merge de feature/vendor-whatsmeow em feature/macbook-lucas (2026-08-07):
# 261 -> 262. Uma issue nova, do metodo GetManyLIDsForPNs portado de
# 3ac8073. max_complexity inalterado (56).
count=267

# tooling: golangci-lint v2.5.0 -> v2.12.2 (2026-08-11): 263 -> 267.
#
# +4 e' EFEITO DA FERRAMENTA, nao de codigo. Medido, nao deduzido: a v2.12.2
# foi rodada em 4d2532e — o HEAD anterior a branch do wa-headless, ainda em
# Go 1.25 e sem nenhuma linha de internal/wa-headless/ — e reportou 267.
#
#   4d2532e + v2.5.0   263 issues   max_complexity 56
#   4d2532e + v2.12.2  267 issues   max_complexity 56
#
# Os 4 novos sao de codigo PRE-EXISTENTE que a versao antiga nao reportava:
# 2 de `gofmt` (pkg/bootstrap/config.go, pkg/presentation/http/handlers/
# handler_session_test.go) — a v2.12 roda formatters que a v2.5 nao rodava —
# e 2 de staticcheck. Ficam registrados aqui e nao foram corrigidos: sao
# anteriores a esta branch, conforme a politica do repo.
#
# max_complexity INALTERADO (56) nas duas versoes. A trava real nao se moveu,
# e e' por isso que a atualizacao nao afrouxa nada: a contagem e' informativa
# por desenho (ver o cabecalho deste arquivo).
#
# Por que a v2.12.2: a v2.5.0 nao consegue mais carregar este modulo. Ela
# embute o go/types do Go com que foi compilada (1.25.1) e recusa pacotes que
# declarem um Go mais novo; o modulo esta em 1.26 desde o chromedp v0.16.0.
# Nenhuma release publicada e' compilada com 1.26 — nem a v2.12.2 — entao o
# Makefile passou a COMPILAR a versao fixada com o Go do repositorio
# (`make lint-tool`). Ver o comentario de GOLANGCI_VERSION no Makefile.

# f17-vet-lint (2026-08-07): 83->284. Ratchet de CONTAGEM apenas — a trava
# real (max_complexity) nao mudou. internal/wa-noise/ entrou em VET_TARGETS e
# LINT_TARGETS (F17 em HOUSEKEEP.md). A premissa registrada na F17 era que o
# gocyclo maximo do modulo estaria muito acima do baseline; medido, ele e' 46
# contra os 56 do repo, entao o gate aguenta sem afrouxar nada. `go vet` no
# modulo ja' saia limpo. Cobertura continua de fora, de proposito: incluir o
# modulo obrigaria a BAIXAR min_coverage.
